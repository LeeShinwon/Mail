import 'package:flutter/cupertino.dart';

class SentMailCard extends StatelessWidget {
  const SentMailCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

